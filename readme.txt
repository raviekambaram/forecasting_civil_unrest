This is an example too identify the social unrest problem at a particular place.
This example finds the stop words and that mainly predicts the unrest situation which is then used to 
build the model and predict the scenario from twitter feeds.
ML Algorithm used : Logistic regression.